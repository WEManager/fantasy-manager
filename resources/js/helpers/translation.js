export function useLanguage(ziggy) {
  console.log("🚀 ~ file: translation.js ~ line 2 ~ useLanguage ~ ziggy", ziggy)

  const __ = (key, replace = {}) => {
    const keys = key.split('.')
    
    // const translation

    // return translation
  }

  return {
    __
  }
}